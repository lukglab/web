apiVersion: v1
kind: Service
metadata:
    name: "mykubernetes"
    labels:
        app: "mykubernetes"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "mykubernetes"